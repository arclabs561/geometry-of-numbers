---
description: Markdown math + lint triage (Covolume)
alwaysApply: true
---

## Markdown math

- Use GitHub’s KaTeX support via:
  - inline: `$ ... $`
  - block: `$$ ... $$`
- Prefer **minimal** formulas that encode the invariant we keep referencing (e.g. `n = 4^a(8k+7)`).
- Keep prose + math aligned: define symbols once, then use them consistently across docs.

## Lint triage policy (Lean / Mathlib)

### Tooling we actually run

- **Style**: `lake exe lint-style`
- **Build**: `lake build`
- **Checks entrypoint (canonical)**: `./Scripts/check.sh pre-commit` / `./Scripts/check.sh pre-push`
- **Checks (Lake wrapper)**: `lake exe gon_precommit` (runs `./Scripts/check.sh pre-commit`)
- (Optional, interactive) **Lean lints**: `#lint` / `#lint_mathlib` in a scratch file when diagnosing suspicious patterns.

### High-signal (usually fix when you touch the code nearby)

- `linter.unusedSimpArgs`: remove unused simp arguments (it’s often just noise + proof drift).
- `linter.unusedArguments` / unused locals: delete or underscore if genuinely unused.
- Any warning that indicates a **wrong lemma name / changed API** (treat as correctness risk).

### Medium-signal (fix if it improves readability)

- `linter.unnecessarySimpa`: change `simpa` → `simp` **only** if the proof stays readable and no extra casts/unfolding appear.

## Lean failure mode: recursion depth / simp loops

If Lean reports “maximum recursion depth has been reached” (often from `simp`), prefer structural fixes:

- Avoid “fixing” by raising limits (`set_option maxRecDepth ...`).
- Extract the looping rewrite into a named lemma, apply via `rw`/`calc`, then use a small `simp` set or `ring_nf`.

### Low-signal (explicitly allowed in this repo)

- `declaration uses 'sorry'` inside `Experiments/` or marked scaffolds, provided the file still builds.

## LLM review (local only)

- `Scripts/check.sh pre-commit` runs an LLM-based review by default (non-blocking if it fails).
  - Disable: `GON_LLM_REVIEW=0` (also accepts `COVOLUME_LLM_REVIEW=0`)
  - Make failures block: `GON_LLM_REVIEW_STRICT=1` (also accepts `COVOLUME_LLM_REVIEW_STRICT=1`)
  - Tune strength: `GON_LLM_REVIEW_POWER=low|high|max` (also accepts `COVOLUME_LLM_REVIEW_POWER=...`)

## Determinism / layering

- `lake build` should stay deterministic + offline.
- Never make `lake build` depend on networked checks (LLM review, web search).
  Use explicit commands (`./Scripts/check.sh ...`, `lake exe covolume_precommit`) instead.

## Case-sensitive paths (Linux/CI)

- `Scripts/` is canonical (capital S). On macOS you may be able to *type* `scripts/` and hit the
  same directory, but CI/Linux will not. Write paths as `Scripts/...` in docs and scripts.

## When to suppress a linter (rare)

- Prefer to fix the proof instead of disabling a linter.
- If a linter is consistently noisy and not actionable in a file, use a **local** `set_option` with a short comment:
  - why it’s noisy here
  - what would make it actionable
  - example: `set_option linter.unnecessarySimpa false`

## Research / iteration hygiene (so we can keep iterating)

- Prefer **local** searches (repo + vendored Mathlib under `covolume/.lake/packages/mathlib/Mathlib`) before web/GitHub search.
- If you use `gh search code`, expect rate limits; keep queries minimal and record “found/not found” in a short chat note.
- When a `simp` recursion-depth failure shows up while doing glue work, treat it as a signal to add a named rewrite lemma.

